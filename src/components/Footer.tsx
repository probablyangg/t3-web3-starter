const Footer = () => {
  return (
    <div
      className={`
    w-full
    bg-primary py-5
    font-secondary
    text-primary-muted
    `}
    >
      <div className="flex flex-row items-center justify-center">
      {/* @todo */}
      </div>
    </div>
  );
};

export default Footer;
